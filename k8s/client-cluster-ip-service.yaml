apiVersion: v1
kind: Service
metadata:
  name: client-cluster-ip-service
spec:
  type: ClusterIP
  selector:
    components: web
  ports:
    # outside port access
    - port: 3000
      targetPort: 3000