apiVersion: apps/v1
kind: Deployment
metadata:
  name: server-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      components: server
  template:
    metadata:
      labels:
        components: server
    spec:
      containers:
        - name: api
          image: stephengrider/multi-server
          ports:
            - containerPort: 5000