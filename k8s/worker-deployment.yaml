apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      components: worker
  template:
    metadata:
      labels:
        components: worker
    spec:
      containers:
        - name: worker
          image: stephengrider/multi-worker